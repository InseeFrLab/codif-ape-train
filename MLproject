name: ape-classifier

entry_points:
  main:
    parameters:
      script_args_1: {type: str, default: ""}
      script_args_2: {type: str, default: ""}

    command: >
      python src/train.py -m {script_args_1} {script_args_2}
